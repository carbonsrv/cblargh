-- Settings for CBlargh

-- General stuff about the blog.
local title = "Muh blargh!"
local aboutme = "Random guy in a random universe on a random blog."

-- This is the path where your posts are stored.
local posts_path = "posts/"

-- Choose your template pack here.
local template_pack = "nothing"

-- Do not change anything after this.

return {
	["title"] = title,
	["posts_path"] = posts_path,
	["template_pack"] = template_pack,
	["aboutme"] = aboutme
}
